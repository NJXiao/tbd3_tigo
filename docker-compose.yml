version: '3.9'

services:

  postgres:

    image: postgres:16

    container_name: postgres_secure

    restart: always

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Admin123
      POSTGRES_DB: empresa

    ports:
      - "5432:5432"